{
  "name": "traffic-analytics-sdk",
  "version": "1.0.0",
  "description": "Zero-dependency client telemetry SDK for traffic analytics",
  "main": "dist/tracker.js",
  "types": "index.d.ts",
  "files": [
    "src/",
    "dist/",
    "index.d.ts"
  ],
  "scripts": {
    "prebuild": "if not exist dist mkdir dist",
    "build": "npm run build:dev",
    "build:dev": "type src\\session-hasher.js src\\command-dispatcher.js src\\tracker.js > dist\\tracker.js",
    "build:prod": "npm run build:dev && terser dist/tracker.js -o dist/tracker.min.js --compress --mangle"
  },
  "keywords": [
    "analytics",
    "telemetry",
    "tracking",
    "session",
    "fingerprinting"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "terser": "^5.27.0"
  }
}