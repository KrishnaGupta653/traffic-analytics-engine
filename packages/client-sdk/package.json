{
  "name": "traffic-analytics-sdk",
  "version": "1.0.0",
  "description": "Zero-dependency client telemetry SDK for traffic analytics",
  "main": "dist/tracker.js",
  "types": "index.d.ts",
  "files": [
    "src/",
    "dist/",
    "index.d.ts"
  ],
  "scripts": {
    "build": "npm run build:dev",
    "build:dev": "cat src/session-hasher.js src/command-dispatcher.js src/tracker.js > dist/tracker.js",
    "build:prod": "echo 'Install terser globally: npm i -g terser' && terser dist/tracker.js -o dist/tracker.min.js --compress --mangle || echo 'Skipping minification - terser not installed'"
  },
  "keywords": [
    "analytics",
    "telemetry",
    "tracking",
    "session",
    "fingerprinting"
  ],
  "author": "",
  "license": "MIT"
}