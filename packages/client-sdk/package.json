{
  "name": "traffic-analytics-sdk",
  "version": "1.0.0",
  "description": "Zero-dependency client telemetry SDK for traffic analytics",
  "main": "dist/tracker.js",
  "types": "index.d.ts",
  "files": [
    "src/",
    "dist/",
    "index.d.ts"
  ],
  "scripts": {
    "prebuild": "node -e \"const fs = require('fs'); if (!fs.existsSync('dist')) fs.mkdirSync('dist', { recursive: true });\"",
    "build": "node build.js",
    "build:prod": "npm run build && npx terser dist/tracker.js -o dist/tracker.min.js --compress --mangle",
    "watch": "node build.js --watch"
  },
  "keywords": [
    "analytics",
    "telemetry",
    "tracking",
    "session",
    "fingerprinting"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "terser": "^5.27.0"
  }
}